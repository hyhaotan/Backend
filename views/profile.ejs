<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Profile - DriveX</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <style>
      /* Đặt phông chữ mặc định, margin, padding */
      body {
        font-family: Arial, sans-serif;
        background-color: #f8f9fa; /* Màu nền nhạt */
        margin: 0;
        padding: 0;
      }

      /* Thanh điều hướng cố định trên cùng */
      nav.navbar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
      }

      /* Chỉnh lại màu chữ cho navbar-brand và nav-link */
      .navbar-brand, .nav-link {
        color: #fff !important;
      }

      /* Style cho phần nội dung chính (profile) */
      .profile-section {
        margin-top: 80px; /* chừa chỗ cho navbar */
      }

      /* Card profile */
      .profile-card {
        max-width: 450px;
        margin: 0 auto; /* Căn giữa ngang */
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }

      .profile-card img {
        width: 150px;
        height: 150px;
        object-fit: cover;
        border-radius: 50%;
        margin-top: -75px;
        border: 5px solid #fff; /* Tạo đường viền trắng cho avatar */
      }

      /* Container cho ảnh avatar */
      .avatar-container {
        position: relative;
        text-align: center;
        margin-bottom: 50px;
      }

      /* Tiêu đề */
      .profile-card h2 {
        margin-top: 0.5rem;
        margin-bottom: 0.5rem;
      }

      /* Nút Chỉnh sửa thông tin */
      .edit-btn {
        margin-top: 1rem;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="#">DriveX</a>
        <button
          class="navbar-toggler d-lg-none"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#collapsibleNavId"
          aria-controls="collapsibleNavId"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavId">
          <ul class="navbar-nav ms-auto mt-2 mt-lg-0 fs-6">
            <li class="nav-item">
              <a class="nav-link active" href="home.html">HOME</a>
            </li>
            <li id="avatarLi" class="nav-item dropdown" style="display: none">
              <a
                class="nav-link dropdown-toggle"
                href="#"
                id="avatarDropdown"
                role="button"
                data-bs-toggle="dropdown"
                aria-expanded="false"
              >
                <div id="avatar" class="avatar"></div>
              </a>
              <ul
                class="dropdown-menu dropdown-menu-end"
                aria-labelledby="avatarDropdown"
              >
                <li>
                  <a class="dropdown-item" href="/profile">Profile</a>
                </li>
                <li><hr class="dropdown-divider" /></li>
                <li>
                  <button id="logoutBtn" class="dropdown-item">Log Out</button>
                </li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Profile Section -->
    <div class="container profile-section">
      <div class="profile-card py-5 px-4">
        <div class="avatar-container">
          <!-- Avatar người dùng -->
          <img
            id="avatar"
            src="<%= user.avatar || '/assets/default.jpg' %>"
            alt="User Avatar"
          />
        </div>
        <div class="text-center">
          <!-- Thông tin người dùng -->
          <h2><%= user.username %></h2>
          <p class="mb-1"><strong>Email:</strong> <%= user.email %></p>
          <p class="mb-1"><strong>Tuổi:</strong> <%= user.age %></p>

          <!-- Nút chỉnh sửa thông tin -->
          <a href="/editprofile" class="btn btn-primary edit-btn">
            Chỉnh sửa thông tin
          </a>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS (nếu cần dùng cho dropdown, modal,...) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-pYQAU5+AiQENABBOAvBlS5ig8tfZ6GDe9TCrH2xd98Yo9Zh1ZpN64LwORlYs9yS1"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
